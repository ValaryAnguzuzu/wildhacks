node_modules
.DS_Store

# Env (secrets) — keep Firebase keys and API tokens out of git
.env
.env.local
.env.*.local

dist
dist-ssr
*.local
node_modules/*
coverage
docs/

# Local IDE
.cursor/

# GCP / Firebase Admin (seed scripts) — never commit real keys
scripts/serviceAccountKey.json